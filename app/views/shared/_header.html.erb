<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">CampRecipe</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">TOP</a>
        </li>
        <li class="nav-item">
          <%= link_to 'レシピ作成', new_recipe_path, class: "nav-link" %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to current_user.nickname, "/", class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'マイページ', "/users/#{current_user.id}", class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'ログイン', new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to '新規登録', new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
      <%= form_with(url: search_recipes_path, local: true, method: :get) do |form| %>
      <div class="d-flex">
        <%= form.text_field :keyword, placeholder: "レシピを検索", class: "form-control me-2" %>
        <%= form.submit "検索", class: "btn btn-outline-success" %>
      </div>
      <% end %>
    </div>
  </div>
</nav>